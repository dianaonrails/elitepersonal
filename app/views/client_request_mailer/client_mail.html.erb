Our suggestions to your request:

<% @candidates.each do |candidate| %>
   <table>
	   	<tr>
	   		<td>
				<%= candidate.first_name %>	
			</td>
			<td>	
				<%= candidate.last_name %>
			</td>
			<td>
				<%= url_for(:controller => "candidates", :action => "show",:only_path => false, :id => candidate.id, :host => "http://elitepersonalworld.herokuapp.com/")  %>
			</td>
			<td>
				Schedule Interview
				<%= url_for(:controller => "interviews", :action => "new", candidate: candidate.id,client_request: @client_request, :escape => false, :host => "http://elitepersonalworld.herokuapp.com/") %>
			</td>
		</tr>

	</table>		
<% end %>